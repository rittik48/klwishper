export default async function PostPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; return <main className="font-ui mx-auto max-w-3xl px-6 py-16"><p className="text-xs font-bold uppercase tracking-widest text-[var(--teal)]">Conversation {id}</p><h1 className="mt-3 font-serif text-4xl">Anonymous thread</h1><p className="mt-5 text-[var(--muted)]">Replies are paginated and identities remain private.</p></main>; }
