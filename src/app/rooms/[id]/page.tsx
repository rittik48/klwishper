export default async function RoomPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; return <main className="font-ui mx-auto max-w-4xl px-6 py-16"><p className="text-xs font-bold uppercase tracking-widest text-[var(--teal)]">Room</p><h1 className="mt-3 font-serif text-5xl capitalize">{id}</h1><p className="mt-5 text-[var(--muted)]">Sign in to read and add anonymous posts in this room.</p></main>; }
