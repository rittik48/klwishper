const rooms = ["General", "College", "Departments", "Subjects", "Hostel", "Events"];
export default function RoomsPage() { return <main className="font-ui mx-auto max-w-5xl px-6 py-16"><p className="text-xs font-bold uppercase tracking-widest text-[var(--teal)]">Rooms</p><h1 className="mt-3 font-serif text-5xl">Find your corner of campus.</h1><div className="mt-10 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{rooms.map((room) => <a href={`/rooms/${room.toLowerCase()}`} key={room} className="rounded-2xl border border-[var(--line)] bg-white p-6 font-bold hover:border-[var(--teal)]">{room}<span className="mt-8 block text-sm font-normal text-[var(--muted)]">Browse anonymous conversations →</span></a>)}</div></main>; }
